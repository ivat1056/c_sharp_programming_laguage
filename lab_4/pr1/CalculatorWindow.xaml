<!-- Главное окно калькулятора-минёра -->
<Window x:Class="MinesweeperCalculator.CalculatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Калькулятор" 
        Height="610" 
        Width="340"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#000000"
        x:Name="MainWindowRoot">
    <Window.RenderTransform>
        <RotateTransform x:Name="WindowRotateTransform" Angle="0"/>
    </Window.RenderTransform>
    <Window.RenderTransformOrigin>
        <Point X="0.5" Y="0.5"/>
    </Window.RenderTransformOrigin>
    <!-- Общие ресурсы: градиенты и стили кнопок -->
    <Window.Resources>
        <LinearGradientBrush x:Key="AppBackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0D0D0F" Offset="0"/>
            <GradientStop Color="#13151B" Offset="0.5"/>
            <GradientStop Color="#0D0D0F" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="CardBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#141821" Offset="0"/>
            <GradientStop Color="#10131A" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="CalculatorButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="65"/>
            <Setter Property="Height" Value="65"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="50"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="1.03" ScaleY="1.03"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="FunctionButtonStyle" TargetType="Button" BasedOn="{StaticResource CalculatorButtonStyle}">
            <Setter Property="Background" Value="#A6A6A6"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>
        
        <Style x:Key="OperatorButtonStyle" TargetType="Button" BasedOn="{StaticResource CalculatorButtonStyle}">
            <Setter Property="Background" Value="#FF9500"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        
        <Style x:Key="NumberButtonStyle" TargetType="Button" BasedOn="{StaticResource CalculatorButtonStyle}">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>
    
    <Grid Name="MainContentGrid" Margin="10" Background="{StaticResource AppBackgroundGradient}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель управления -->
        <WrapPanel Grid.Row="0" 
                   Orientation="Horizontal" 
                   HorizontalAlignment="Right"
                   ItemHeight="40"
                   ItemWidth="130"
                   Margin="0,0,0,10">
            <Button Name="MinimizeButton"
                    Content="➖"
                    Click="MinimizeButton_Click"
                    Margin="0,0,10,0"
                    Padding="15,8"
                    FontSize="16"
                    FontWeight="Bold"
                    Background="#333333"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    ToolTip="Свернуть окно">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="20"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            
            <Button Name="ExpandButton"
                    Content="⛶"
                    Click="ExpandButton_Click"
                    Margin="0,0,10,0"
                    Padding="15,8"
                    FontSize="16"
                    FontWeight="Bold"
                    Background="#333333"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    ToolTip="Расширить/Свернуть окно">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="20"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            
            <Button Name="HintButton"
                    Content="💡 Подсказка"
                    Click="HintButton_Click"
                    Margin="0,0,10,0"
                    Padding="15,8"
                    FontSize="14"
                    FontWeight="Bold"
                    Background="#444444"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    ToolTip="Открыть безопасную клетку">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="20"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <CheckBox x:Name="ShowHintsCheckBox"
                      Content="Показывать подсказки (1-8)"
                      Foreground="White"
                      FontWeight="Medium"
                      VerticalAlignment="Center"
                      Margin="0,0,10,0"
                      IsChecked="True"
                      Checked="ShowHintsCheckBox_Checked"
                      Unchecked="ShowHintsCheckBox_Checked"/>
            
            <Button Name="ResetButton"
                    Content="🔄 Новая игра"
                    Click="ResetButton_Click"
                    Padding="15,8"
                    FontSize="14"
                    FontWeight="Bold"
                    Background="#333333"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Visibility="Collapsed">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="20"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </WrapPanel>

        <!-- Экран/дисплей -->
        <Border Grid.Row="1" 
                Background="#0E0F13" 
                Padding="20,30,20,10">
            <Border.Effect>
                <DropShadowEffect Color="#202020" BlurRadius="16" ShadowDepth="0" Opacity="0.6"/>
            </Border.Effect>
            <TextBlock Name="DisplayLabel" 
                      Text="0" 
                      FontSize="64"
                      FontWeight="Light"
                      Foreground="#FFFFFF"
                      TextAlignment="Right"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Bottom"
                      FontFamily="SF Pro Display, Segoe UI"/>
        </Border>

        <TextBlock Name="OopsLabel"
                   Grid.Row="1"
                   Grid.RowSpan="2"
                   Text="упс..."
                   FontSize="120"
                   FontWeight="Bold"
                   Foreground="#FF9500"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Collapsed"
                   Opacity="0">
        </TextBlock>

        <!-- Область с кнопками-ячейками -->
        <Border Grid.Row="2"
                Background="{StaticResource CardBackground}"
                CornerRadius="16"
                Padding="12"
                Margin="0,6,0,0">
            <Border.Effect>
                <DropShadowEffect Color="#111111" BlurRadius="18" ShadowDepth="0" Opacity="0.55"/>
            </Border.Effect>
            <Grid Name="GameGrid"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
            </Grid>
        </Border>
    </Grid>
</Window>

